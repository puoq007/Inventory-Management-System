{
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "scripts": {
    "dev": "concurrently -n \"API,CLIENT\" -c \"blue,green\" \"npm run api\" \"npm run client\"",
    "api": "dotnet watch run --project ./backend --urls \"http://localhost:5105\"",
    "client": "dotnet watch run --project ./frontend --urls \"http://localhost:5196\""
  }
}